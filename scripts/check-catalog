#!/usr/bin/env bash
# Validate the machine-readable product registry, product repos, and generated catalogs.
set -euo pipefail
ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd -P)"
exec python3 "$ROOT/scripts/product_registry.py" check
