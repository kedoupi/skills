# Artifacts

Generated outputs for this skill repo (not installable).

```text
artifacts/
└── live/<suite>/<version>/
    ├── report.md
    └── … generated files …
```

Local scratch: `artifacts/live/_scratch/` (gitignored in template).

Specs live under `tests/live/`; pixels and dumps live here.
