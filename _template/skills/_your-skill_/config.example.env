# Copy via:
#   bash scripts/<skill-name> init --chat-id <id>
#
# Durable path (outside skill package, survives skills update):
#   <skills-parent>/.skill-data/<skill-name>/config.env

# Required
SKILL_CHAT_ID='oc_xxxxxxxx'

# Optional
SKILL_AS='bot'
SKILL_FOOTER='via <skill-name>'
